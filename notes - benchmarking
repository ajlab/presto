Benchmarking
Test Queries
1) 
Query: show tables;
Type: System
Runtime:
With cache: < .001
Without cache: .02

ii)
Query: select count(1) from users;
System: Azure 3.75G RAM, 3-node Hadoop
Runtime:
Without Cache: 0.04

iii)
Query: select name from users where name like '%aro%' group by name;
System: Azure 3.75G RAM, 3-node Hadoop
Type: like
Runtime:
Without Cache: 0.03

iv) 
Query:
select review.user_id, count(distinct review_id) from review, users where review.user_id=users.user_id group by review.user_id;
select review.user_id, count(distinct review_id) from review, users where review.user_id=users.user_id and review.date>'2018-06-01' group by review.user_id;
Type: 1-Join
System: Azure 3.75G RAM, 3-node Hadoop
Runtime:
Without Cache:
Insufficient Resources

v) 
Query:
select review.user_id, count(distinct review_id) from review, users where review.user_id=users.user_id and review.date>'2018-06-01' group by review.user_id;
Type: 1-Join
System: Azure 3.75G RAM, 3-node Hadoop
Runtime:
Without Cache: 12s
